[package]
name = "ui"
version = "0.1.0"
edition = "2024"

[dependencies]
engine = { path = "../engine" }
utils = { path = "../utils" }
tokio.workspace = true
tracing.workspace = true
iced = { version = "0.13.1", features = ["tokio", "svg", "image", "system"] }
iced_futures = "0.13.2"